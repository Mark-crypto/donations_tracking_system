<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
         #donation {
  font-family: Arial, Helvetica, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

#donation td, #donation th {
  border: 1px solid #ddd;
  padding: 8px;
}

#donation tr:nth-child(even){background-color: #f2f2f2;}

#donation tr:hover {background-color: #ddd;}

#donation th {
  padding-top: 12px;
  padding-bottom: 12px;
  text-align: left;
  background-color: #04AA6D;
  color: white;
}
    </style>
</head>
<body>
    <h1 style="color: green;text-align:center;text-decoration:underline">ITEMS DONATED</h1>
    <a href="/donation-view/export"><button style="height: 40px; width:180px; background-color:#04AA6D; border: none; border-radius: 8px; color: white; font-weight: bold;">Download csv file</button></a><br><br>
    <% if (action == 'add') { %>
     
        <% } else{ %>
            <table id="donation">
                
                    <tr>
                        <th>Item</th>
                        <th>Total Number(bags)</th>
                        <th>Dispatch</th>
                        <th>Destination</th>
                        <th>Month</th>
                        <th>Region</th>
                        
                    </tr>
                <% if (donData.length> 0) { 
                    donData.forEach(data => {
                    
                    %>
                    <tr >
                        <td ><%= data.name %></td>
                        <td ><%= data.totalNumber %></td>
                        <td ><%= data.dispatch %></td>
                        <td ><%= data.destination %></td>
                        <td ><%= data.month %></td>
                        <td ><%= data.region %></td>
                    </tr>
                    <%
                    });
                 } else{ %>
                    <tr><td>No Data found</td></tr>
                <% } %>
    
            </table>
        <% } %>

</body>
</html>